@page "/login"
@inherits LoginBase

<section class="login-section">
    <h3>Login</h3>
    <EditForm Model="loginModel" OnValidSubmit="OnLogin">
        <DataAnnotationsValidator />
        <ValidationSummary />
        <div class="form">
            <div class="username">
                <InputText @bind-Value="loginModel.UsernameOrEmail " placeholder="Username or Email" class="form-control" />
                <ValidationMessage For="@(() => loginModel.UsernameOrEmail)" />
            </div>
            <div class="password">
                <InputText @bind-Value="loginModel.Password" type="password" placeholder="Password" class="form-control" />
                <ValidationMessage For="@(() => loginModel.Password)" />
            </div>
            <div class="button">
                <button type="submit" class="btn btn-lg login-button">Login</button>
            </div>
        </div>
        @if (!string.IsNullOrEmpty(errorMessage))
        {
            <div class="alert alert-danger">@errorMessage</div>
        }
    </EditForm>
</section>
